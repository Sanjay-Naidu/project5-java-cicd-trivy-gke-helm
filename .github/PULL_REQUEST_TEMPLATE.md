## What changed

<!-- One or two sentences. -->

## Why

<!-- The problem this solves / the decision behind it. -->

## Checklist

- [ ] `mvn verify` passes locally (or I am relying on PR Validation)
- [ ] Helm values changed? Both `values-dev.yaml` and `values-prod.yaml` reviewed
- [ ] No secrets, keys or project-specific IDs committed
- [ ] README / docs updated if behaviour or setup changed
